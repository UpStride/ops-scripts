<source>
 @type forward
 port 24225
</source>

<match docker.**>
  @type copy
  <store>
    @type elasticsearch
    host us-east1.watch.upstride.io
    user upstride-agent
    password WvRsfEZLBqGn5c3fhdyR
    port 9000
    logstash_format true
    logstash_prefix fluentd
    logstash_dateformat %Y%m%d
    include_tag_key true
    type_name fluentd
    tag_key @log_name
    flush_interval 1s
  </store>
  <store>
    @type stdout
  </store>
</match>
